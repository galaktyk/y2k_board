diff --git a/src/input/handlers/mouse.rs b/src/input/handlers/mouse.rs
index 25505ae..0602896 100644
--- a/src/input/handlers/mouse.rs
+++ b/src/input/handlers/mouse.rs
@@ -68,6 +68,7 @@ fn begin_transform_drag(
 ) {
     state.pending_drag_mode = DragMode::None;
     state.drag_mode = drag_mode;
+    state.clear_resize_preview();
     state.move_origin = board
         .elements
         .iter()
@@ -94,6 +95,7 @@ fn clear_pending_drag(state: &mut InputState) {
 fn begin_marquee_drag(state: &mut InputState, world: Vec2) {
     state.pending_drag_mode = DragMode::None;
     state.drag_mode = DragMode::MarqueeSelect;
+    state.clear_resize_preview();
     state.move_start_world = world;
     state.move_delta = Vec2::ZERO;
     state.marquee_bounds = Some(SelectionBounds::from_points(world, world));   
@@ -105,6 +107,7 @@ fn begin_marquee_drag(state: &mut InputState, world: Vec2) {
 fn begin_connection_drag(state: &mut InputState, connection_drag: ConnectionDra
g) {                                                                                 state.pending_drag_mode = DragMode::None;
     state.drag_mode = DragMode::CreatingConnection;
+    state.clear_resize_preview();
     state.move_origin.clear();
     state.move_delta = Vec2::ZERO;
     state.rotate_delta = 0.0;
@@ -355,8 +358,7 @@ fn scale_point_from_anchor_in_frame(
     rotate_point(scaled_local, frame_center, frame_rotation)
 }

-fn resize_rotated_element_in_frame(
-    element: &mut Element,
+fn resized_rotated_element_transform_in_frame(
     orig_pos: Vec2,
     orig_size: Vec2,
     orig_rot: f32,
@@ -364,7 +366,7 @@ fn resize_rotated_element_in_frame(
     scale_x: f32,
     scale_y: f32,
     frame_bounds: SelectionBounds,
-) {
+) -> ElementTransform {
     let original_center = orig_pos + orig_size * 0.5;
     let scaled_center = scale_point_from_anchor_in_frame(
         original_center,
@@ -392,35 +394,43 @@ fn resize_rotated_element_in_frame(
         orig_rot
     };

-    element.rotation = rotation;
-    element.size = Vec2::new(width, height);
-    element.pos = scaled_center - element.size * 0.5;
+    let size = Vec2::new(width, height);
+    ElementTransform::new(scaled_center - size * 0.5, size, rotation)
 }

-fn element_corners(element: &Element) -> Vec<Vec2> {
-    if element.shape == ShapeType::Line {
-        return vec![element.pos, element.pos + element.size];
+fn element_corners_from_transform(shape: ShapeType, pos: Vec2, size: Vec2, rota
tion: f32) -> Vec<Vec2> {                                                       +    if shape == ShapeType::Line {
+        return vec![pos, pos + size];
     }

-    let center = element.pos + element.size * 0.5;
-    let half_size = element.size * 0.5;
+    let center = pos + size * 0.5;
+    let half_size = size * 0.5;
     vec![
-        rotate_point(center + Vec2::new(-half_size.x, -half_size.y), center, el
ement.rotation),                                                                -        rotate_point(center + Vec2::new(half_size.x, -half_size.y), center, ele
ment.rotation),                                                                 -        rotate_point(center + Vec2::new(half_size.x, half_size.y), center, elem
ent.rotation),                                                                  -        rotate_point(center + Vec2::new(-half_size.x, half_size.y), center, ele
ment.rotation),                                                                 +        rotate_point(center + Vec2::new(-half_size.x, -half_size.y), center, ro
tation),                                                                        +        rotate_point(center + Vec2::new(half_size.x, -half_size.y), center, rot
ation),                                                                         +        rotate_point(center + Vec2::new(half_size.x, half_size.y), center, rota
tion),                                                                          +        rotate_point(center + Vec2::new(-half_size.x, half_size.y), center, rot
ation),                                                                              ]
 }

-fn selection_bounds_from_selected_elements_in_frame(
+fn selection_bounds_from_preview_transforms(
     board: &Board,
+    preview_transforms: &std::collections::HashMap<u64, ElementTransform>,     
     frame_rotation: f32,
 ) -> Option<SelectionBounds> {
     let mut local_min: Option<Vec2> = None;
     let mut local_max: Option<Vec2> = None;

-    for element in board.elements.iter().filter(|element| element.selected) {  
-        for corner in element_corners(element) {
+    for (&id, transform) in preview_transforms {
+        let Some(element) = board.element(id) else {
+            continue;
+        };
+        for corner in element_corners_from_transform(
+            element.shape,
+            transform.pos,
+            transform.size,
+            transform.rotation,
+        ) {
             let local_corner = inverse_rotate_vector(corner, frame_rotation);  
             local_min = Some(match local_min {
                 Some(current) => current.min(local_corner),
@@ -449,6 +459,43 @@ fn selection_bounds_from_selected_elements_in_frame(       
     })
 }

+fn resize_preview_transform_changes(state: &InputState) -> Vec<ElementPropertyC
hange> {                                                                        +    state
+        .move_origin
+        .iter()
+        .filter_map(|&(id, orig_pos, orig_size, orig_rot)| {
+            let after = state.resize_preview_transform(id)?;
+            let before = ElementTransform::new(orig_pos, orig_size, orig_rot); 
+            (before != after).then_some(ElementPropertyChange {
+                id,
+                patch: ElementPropertyPatch::Transform { before, after },      
+            })
+        })
+        .collect()
+}
+
+fn deferred_resize_text_generations(
+    board: &Board,
+    changes: &[ElementPropertyChange],
+) -> Vec<(u64, u64)> {
+    changes
+        .iter()
+        .filter_map(|change| {
+            let ElementPropertyPatch::Transform { before, after } = &change.pat
ch else {                                                                       +                return None;
+            };
+            if before.size == after.size {
+                return None;
+            }
+            let element = board.element(change.id)?;
+            element
+                .text
+                .as_ref()
+                .map(|_| (change.id, element.text_layout_generation))
+        })
+        .collect()
+}
+
 fn rect_edge_anchor(norm_pos: Vec2) -> Vec2 {
     let mut snapped = norm_pos.clamp(Vec2::ZERO, Vec2::ONE);
     let dist_left = snapped.x;
@@ -957,6 +1004,25 @@ pub fn on_mouse_up(
                 }
                 state.selection_bounds = state.drag_selection_bounds;
             }
+            DragMode::ResizingHandle(_) => {
+                let changes = resize_preview_transform_changes(state);
+                if !changes.is_empty() {
+                    let deferred_text = deferred_resize_text_generations(board,
 &changes);                                                                     +                    board.apply_operation(BoardOperation::SetProperty {        
+                        changes,
+                        sync_connected_lines: true,
+                    });
+                    for (id, generation) in deferred_text {
+                        if let Some(element) = board.element_mut(id) {
+                            element.text_layout_generation = generation;       
+                        }
+                        state.enqueue_resize_text_recompute(id);
+                    }
+                }
+                if state.move_origin.len() > 1 {
+                    state.selection_bounds = state.drag_selection_bounds;      
+                }
+            }
             _ => {
                 let changes = board.selected_transform_changes(&state.move_orig
in);                                                                                             if !changes.is_empty() {
@@ -992,6 +1058,7 @@ pub fn on_mouse_up(
     state.marquee_bounds = None;
     state.drag_selection_bounds = None;
     state.transform_bounds_origin = None;
+    state.clear_resize_preview();

     if state.dragging_tool {
         state.dragging_tool = false;
@@ -1148,33 +1215,34 @@ pub fn on_mouse_move(
             return;
         }

-        for element in &mut board.elements {
-            if element.selected {
-                if let Some(&(_, orig_pos, orig_size, orig_rot)) = state       
-                    .move_origin
-                    .iter()
-                    .find(|&&(id, _, _, _)| id == element.id)
-                {
-                    match state.drag_mode {
-                        DragMode::Rotating => {
-                            if !is_group_transform {
-                                let center = orig_pos + orig_size * 0.5;       
-                                let angle_diff = rotation_angle_delta(state.mov
e_start_world, world, center);                                                  -                                element.rotation = orig_rot + angle_diff;      
-                            }
+        let origins = state.move_origin.clone();
+        for (id, orig_pos, orig_size, orig_rot) in origins {
+            let Some(element_shape) = board.element(id).map(|e| e.shape) else {
+                continue;
+            };
+
+            match state.drag_mode {
+                DragMode::Rotating => {
+                    if !is_group_transform {
+                        if let Some(element) = board.element_mut(id) {
+                            let center = orig_pos + orig_size * 0.5;
+                            let angle_diff = rotation_angle_delta(state.move_st
art_world, world, center);                                                      +                            element.rotation = orig_rot + angle_diff;
                         }
-                        DragMode::ResizingHandle(dir) => {
-                            if is_group_transform {
-                                let Some(bounds) = state.transform_bounds_origi
n else {                                                                        -                                    continue;
-                                };
-                                let Some((anchor, scale_x, scale_y)) =
-                                    group_resize_from_handle(bounds, dir, world
)                                                                               -                                else {
-                                    continue;
-                                };
-
-                                if element.shape == ShapeType::Line {
+                    }
+                }
+                DragMode::ResizingHandle(dir) => {
+                    let preview_transform = if is_group_transform {
+                        let Some(bounds) = state.transform_bounds_origin else {
+                            continue;
+                        };
+                        let Some((anchor, scale_x, scale_y)) =
+                            group_resize_from_handle(bounds, dir, world)       
+                        else {
+                            continue;
+                        };
+
+                        if element_shape == ShapeType::Line {
                                     let start = scale_point_from_anchor_in_fram
e(                                                                                                                       orig_pos,
                                         anchor,
@@ -1191,11 +1259,9 @@ pub fn on_mouse_move(
                                         bounds.center(),
                                         bounds.rotation,
                                     );
-                                    element.pos = start;
-                                    element.size = end - start;
+                                    ElementTransform::new(start, end - start, o
rig_rot)                                                                                                         } else {
-                                    resize_rotated_element_in_frame(
-                                        element,
+                                    resized_rotated_element_transform_in_frame(
                                         orig_pos,
                                         orig_size,
                                         orig_rot,
@@ -1203,19 +1269,19 @@ pub fn on_mouse_move(
                                         scale_x,
                                         scale_y,
                                         bounds,
-                                    );
+                                    )
                                 }
                             } else if element.shape == ShapeType::Line {       
                                 match dir {
                                     HandleDir::LineStart => {
                                         let old_end = orig_pos + orig_size;    
-                                        element.pos = orig_pos + state.move_del
ta;                                                                             -                                        element.size = old_end - element.pos;  
+                                        let pos = orig_pos + state.move_delta; 
+                                        ElementTransform::new(pos, old_end - po
s, orig_rot)                                                                                                         }
                                     HandleDir::LineEnd => {
-                                        element.size = orig_size + state.move_d
elta;                                                                           +                                        ElementTransform::new(orig_pos, orig_si
ze + state.move_delta, orig_rot)                                                                                     }
-                                    _ => {}
+                                    _ => ElementTransform::new(orig_pos, orig_s
ize, orig_rot),                                                                                                  }
                             } else {
                                 let c = orig_rot.cos();
@@ -1272,12 +1338,9 @@ pub fn on_mouse_move(
                                 let w_center =
                                     orig_pos + orig_size * 0.5 + Vec2::new(w_dc
x, w_dcy);                                                                      
-                                element.size = new_size;
-                                element.pos = w_center - new_size * 0.5;       
-                            }
-                            if element.text.is_some() {
-                                state.enqueue_resize_text_recompute(element.id)
;                                                                               -                            }
+                                ElementTransform::new(w_center - new_size * 0.5
, new_size, orig_rot)                                                           +                            };
+                            state.set_resize_preview_transform(element.id, prev
iew_transform);                                                                                          }
                         DragMode::MoveSelected
                         | DragMode::MarqueeSelect
@@ -1290,8 +1353,9 @@ pub fn on_mouse_move(
         if is_group_transform {
             if let DragMode::ResizingHandle(dir) = state.drag_mode {
                 if let Some(bounds) = state.transform_bounds_origin {
-                    state.drag_selection_bounds = selection_bounds_from_selecte
d_elements_in_frame(                                                            +                    state.drag_selection_bounds = selection_bounds_from_preview
_transforms(                                                                                             board,
+                        state.resize_preview_transforms(),
                         bounds.rotation,
                     )
                     .or_else(|| resized_selection_bounds(bounds, dir, world)); 
